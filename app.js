const CONFIG=window.RYUU_CONFIG||{anilist:{clientId:0,redirectUrl:location.origin+location.pathname,graphqlUrl:'https://graphql.anilist.co',authorizeUrl:'https://anilist.co/api/v2/oauth/authorize'}};
const API_URL=CONFIG.anilist.graphqlUrl;
const PLAYER_SOURCES={megaplayAni:{label:'MegaPlay · AniList',build:(a,e,l)=>`https://megaplay.buzz/stream/ani/${a.id}/${e}/${l}`},megaplayMal:{label:'MegaPlay · MAL',build:(a,e,l)=>a.idMal?`https://megaplay.buzz/stream/mal/${a.idMal}/${e}/${l}`:null},anixoAni:{label:'AnixO · AniList',build:(a,e,l)=>`https://anixo.buzz/embed/ani/${a.id}/${e}/${l}?color=%237c5cfc`},megavidAni:{label:'MegaVid · AniList',build:(a,e,l,p)=>`https://megavid.buzz/ani/${a.id}/${e}/${l}?color=%237c5cfc&autoplay=${p}`},megavidMal:{label:'MegaVid · MAL',build:(a,e,l,p)=>a.idMal?`https://megavid.buzz/mal/${a.idMal}/${e}/${l}?color=%237c5cfc&autoplay=${p}`:null}};
const DEFAULT_SETTINGS={glow:true,showHero:true,hideSiteBanner:false,hoverCards:true,reducedMotion:false,defaultLanguage:'sub',preferredSource:'megaplayAni',autoplay:true};
const state={route:'home',returnRoute:'home',featured:[],featureIndex:0,featureTimer:null,hoverTimer:null,searchTimer:null,cardData:new Map(),currentAnime:null,currentEpisode:1,language:'sub',source:'megaplayAni',auth:{token:localStorage.getItem('ryuu-anilist-token')||'',viewer:null},settings:{...DEFAULT_SETTINGS}};
document.addEventListener('DOMContentLoaded',init);
async function init(){state.settings={...DEFAULT_SETTINGS,...readStored('ryuu-settings',{})};state.language=state.settings.defaultLanguage;state.source=state.settings.preferredSource;consumeAnilistCallback();applySettings();setupEvents();setupHeroMotion();setupHeroWordsFill();registerServiceWorker();loadHome();renderSettings();if(state.auth.token)await loadAnilistViewer();updateAnilistUI()}
function setupEvents(){document.addEventListener('click',event=>{const auth=event.target.closest('[data-anilist-auth]');if(auth){toggleAnilistAuth();return}const route=event.target.closest('[data-route]')?.dataset.route;if(route){route==='back'?showRoute(state.returnRoute):showRoute(route);return}const result=event.target.closest('.search-result');if(result){closeSearchDropdown();document.getElementById('global-search').value='';openAnime(Number(result.dataset.animeId));return}const card=event.target.closest('[data-anime-id]');if(card)openAnime(Number(card.dataset.animeId))});const searchInput=document.getElementById('global-search');searchInput.addEventListener('focus',()=>openSearchDropdown());searchInput.addEventListener('input',()=>{clearTimeout(state.searchTimer);const term=searchInput.value.trim();state.searchTimer=setTimeout(()=>runHeaderSearch(term),320)});searchInput.addEventListener('keydown',event=>{if(event.key==='Enter')searchFromInput(searchInput.value.trim());if(event.key==='Escape'){searchInput.blur();closeSearchDropdown()}});document.addEventListener('click',event=>{if(!event.target.closest('.search-wrap'))closeSearchDropdown()});document.getElementById('featured-carousel').addEventListener('mouseenter',stopFeatureAutoplay);document.getElementById('featured-carousel').addEventListener('mouseleave',startFeatureAutoplay);document.addEventListener('keydown',event=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==='k'){event.preventDefault();searchInput.focus();searchInput.select()}if(event.key==='Escape'){hideHoverCard();closeSearchDropdown();showRoute('home')}if(state.route==='home'&&event.key==='ArrowLeft')cycleFeature(-1);if(state.route==='home'&&event.key==='ArrowRight')cycleFeature(1)})}
function openSearchDropdown(){const dd=document.getElementById('search-dropdown');if(dd.innerHTML.trim())dd.hidden=false}
function closeSearchDropdown(){const dd=document.getElementById('search-dropdown');dd.hidden=true;dd.innerHTML=''}
async function runHeaderSearch(term){const dd=document.getElementById('search-dropdown');if(!term){closeSearchDropdown();return}dd.hidden=false;dd.innerHTML='<div class="search-dropdown-loading">Searching…</div>';const query=`query Search($search: String) { Page(perPage: 8) { media(type: ANIME, search: $search, isAdult: false) { id idMal title { romaji english native } coverImage { extraLarge large medium color } bannerImage description(asHtml: false) episodes format status season seasonYear averageScore popularity genres startDate { year month day } trailer { id site thumbnail } nextAiringEpisode { episode airingAt } } } }`;try{const data=await anilist(query,{search:term});renderSearchDropdown(data.Page.media)}catch(error){dd.innerHTML='<div class="search-dropdown-empty">Could not search AniList.</div>'}}
function renderSearchDropdown(list){const dd=document.getElementById('search-dropdown');list.forEach(anime=>state.cardData.set(anime.id,anime));if(!list.length){dd.innerHTML='<div class="search-dropdown-empty">No titles found.</div>';return}dd.innerHTML=list.map(a=>`<button type="button" class="search-result" data-anime-id="${a.id}"><img src="${escapeAttribute(a.coverImage?.medium||coverOf(a))}" alt="" loading="lazy"><span><strong>${escapeHTML(titleOf(a))}</strong><small>${escapeHTML(a.format||'ANIME')} · ${a.episodes||'?'} episodes · ${escapeHTML((a.status||'').replace(/_/g,' '))}</small></span></button>`).join('')}
function consumeAnilistCallback(){const params=new URLSearchParams(location.hash.slice(1)),token=params.get('access_token');if(!token)return;state.auth.token=token;localStorage.setItem('ryuu-anilist-token',token);history.replaceState(null,document.title,location.pathname+location.search);toast('AniList connected.')}
async function toggleAnilistAuth(){if(state.auth.token){state.auth={token:'',viewer:null};localStorage.removeItem('ryuu-anilist-token');updateAnilistUI();renderSettings();toast('AniList disconnected.');return}if(!CONFIG.anilist.clientId){toast('AniList client ID is missing from key.js.',true);return}const url=new URL(CONFIG.anilist.authorizeUrl);url.searchParams.set('client_id',CONFIG.anilist.clientId);url.searchParams.set('response_type','token');location.assign(url.toString())}
async function loadAnilistViewer(){try{const data=await anilist(`query { Viewer { id name avatar { large } statistics { anime { count episodesWatched minutesWatched } } } }`,{});state.auth.viewer=data.Viewer;updateAnilistUI();renderSettings()}catch(error){state.auth={token:'',viewer:null};localStorage.removeItem('ryuu-anilist-token');updateAnilistUI();toast('AniList session expired. Please connect again.',true)}}
function updateAnilistUI(){const button=document.getElementById('anilist-auth');if(!button)return;const viewer=state.auth.viewer;if(viewer){button.classList.add('is-connected');button.innerHTML=`${viewer.avatar?.large?`<img src="${escapeAttribute(viewer.avatar.large)}" alt="">`:''}<span>${escapeHTML(viewer.name)}</span>`;button.title='Disconnect AniList'}else{button.classList.remove('is-connected');button.textContent='AniList';button.title='Connect AniList'}}
function setupHeroMotion(){const hero=document.getElementById('landing-hero');let queued=false;const update=()=>{queued=false;const progress=Math.min(1,Math.max(0,window.scrollY/520));hero.style.setProperty('--hero-progress',progress.toFixed(3));hero.classList.toggle('hero-collapsed',progress>=1)};window.addEventListener('scroll',()=>{if(!queued){queued=true;requestAnimationFrame(update)}},{passive:true});update()}
function fillHeroWords(){document.querySelectorAll('.hero-words[data-repeat]').forEach(el=>{const unit=`${el.dataset.repeat} `;el.textContent=unit;const unitWidth=el.scrollWidth||1;const target=Math.max(window.innerWidth,window.screen?.width||0)*2.5;const copies=Math.max(10,Math.ceil(target/unitWidth)+6);el.textContent=unit.repeat(copies).trim()})}
function setupHeroWordsFill(){fillHeroWords();let queued=false;window.addEventListener('resize',()=>{if(!queued){queued=true;setTimeout(()=>{queued=false;fillHeroWords()},150)}});document.fonts?.ready?.then(fillHeroWords).catch(()=>{})}
async function loadHome(){const query=`query Home { featured: Page(perPage: 8) { media(type: ANIME, sort: TRENDING_DESC, isAdult: false) { ...AnimeCard } } airing: Page(perPage: 12) { media(type: ANIME, sort: POPULARITY_DESC, status: RELEASING, isAdult: false) { ...AnimeCard } } popular: Page(perPage: 12) { media(type: ANIME, sort: POPULARITY_DESC, isAdult: false) { ...AnimeCard } } } fragment AnimeCard on Media { id idMal title { romaji english native } coverImage { extraLarge large medium color } bannerImage description(asHtml: false) episodes format status season seasonYear averageScore popularity genres startDate { year month day } trailer { id site thumbnail } nextAiringEpisode { episode airingAt } }`;try{const data=await anilist(query,{});state.featured=data.featured.media.filter(a=>a.bannerImage).slice(0,6);if(!state.featured.length)state.featured=data.featured.media.slice(0,6);renderFeatured();renderAnimeGrid('top-airing-grid',data.airing.media);renderAnimeGrid('popular-grid',data.popular.media);renderAiring(data.airing.media.slice(0,5));startFeatureAutoplay()}catch(error){showNetworkError(['featured-carousel','top-airing-grid','popular-grid','airing-list'],error)}}
async function openAnime(id){state.returnRoute=state.route==='detail'?state.returnRoute:state.route;showRoute('detail');document.getElementById('detail-content').innerHTML='<div class="loading-block">Loading anime details…</div>';const query=`query Detail($id: Int) { Media(id: $id, type: ANIME) { id idMal title { romaji english native userPreferred } coverImage { extraLarge large medium color } bannerImage description(asHtml: false) episodes duration format status season seasonYear averageScore popularity genres source countryOfOrigin synonyms startDate { year month day } endDate { year month day } trailer { id site thumbnail } nextAiringEpisode { episode airingAt } streamingEpisodes { title thumbnail url site } mediaListEntry { id status progress score(format: POINT_10) } studios(isMain: true) { nodes { name } } relations { edges { relationType(version: 2) node { id type format status episodes seasonYear title { userPreferred } coverImage { medium } } } } } }`;try{state.currentAnime=(await anilist(query,{id})).Media;state.currentEpisode=1;state.language=state.settings.defaultLanguage;state.source=state.settings.preferredSource;state.currentAnime.episodeEntries=episodeEntries(state.currentAnime);renderDetail();window.scrollTo({top:0,behavior:'smooth'})}catch(error){document.getElementById('detail-content').innerHTML=`<div class="empty-state">Could not load this anime. ${escapeHTML(error.message)}</div>`}}
function renderFeatured(){const container=document.getElementById('featured-carousel');const anime=state.featured[state.featureIndex];if(!anime){container.innerHTML='<div class="empty-state">No featured anime found.</div>';return}const title=titleOf(anime),backdrop=anime.bannerImage||coverOf(anime);container.innerHTML=`<div class="feature-shell"><img class="feature-glow" src="${escapeAttribute(backdrop)}" alt=""><article class="feature feature-enter" data-anime-id="${anime.id}" role="button" tabindex="0"><img class="feature-backdrop" src="${escapeAttribute(backdrop)}" alt=""><div class="feature-info"><p class="eyebrow">${escapeHTML(anime.season||'FEATURED')} ${anime.seasonYear||''}</p><h2>${escapeHTML(title)}</h2><p class="feature-native">${escapeHTML(anime.title.native||anime.title.english||'')}</p><p class="feature-desc">${escapeHTML(trimText(anime.description,250)||'No synopsis available.')}</p>${statsMarkup(anime)}<button class="button button-primary" type="button">View title <span>→</span></button></div></article></div>`}
function cycleFeature(direction){if(!state.featured.length)return;state.featureIndex=(state.featureIndex+direction+state.featured.length)%state.featured.length;renderFeatured()}
function startFeatureAutoplay(){stopFeatureAutoplay();if(!state.settings.reducedMotion)state.featureTimer=setInterval(()=>cycleFeature(1),5200)}function stopFeatureAutoplay(){if(state.featureTimer){clearInterval(state.featureTimer);state.featureTimer=null}}
function renderAiring(list){const tile=a=>`<article class="activity-tile" data-anime-id="${a.id}" tabindex="0" role="button"><img src="${escapeAttribute(coverOf(a))}" alt="${escapeAttribute(titleOf(a))}" loading="lazy"><div class="activity-tile-copy"><strong>${escapeHTML(titleOf(a))}</strong><span>${escapeHTML(a.format||'ANIME')} · ${a.episodes||'?'} eps</span><small>★ ${a.averageScore||'—'} · ${compactNumber(a.popularity)} users</small></div></article>`;const left=list.slice(0,3),right=list.slice(3,5);document.getElementById('airing-list').innerHTML=`<div class="activity-lane activity-lane-left">${[...left,...left,...left].map(tile).join('')}</div><div class="activity-lane activity-lane-right">${[...right,...right,...right,...right].map(tile).join('')}</div>`}
function renderAnimeGrid(id,list){const grid=document.getElementById(id);list.forEach(anime=>state.cardData.set(anime.id,anime));grid.innerHTML=list.length?list.map(a=>`<article class="anime-card" data-anime-id="${a.id}" tabindex="0" role="button"><div class="anime-card-art"><img class="card-glow" src="${escapeAttribute(coverOf(a))}" alt=""><div class="anime-poster"><img src="${escapeAttribute(coverOf(a))}" alt="${escapeAttribute(titleOf(a))}" loading="lazy"><span class="card-score">★ ${a.averageScore||'—'}</span></div></div><div class="anime-card-info"><div class="anime-card-title">${escapeHTML(titleOf(a))}</div><div class="anime-card-meta">${escapeHTML(a.format||'ANIME')} · ${a.episodes||'?'} episodes</div></div></article>`).join(''):'<div class="empty-state">No titles found.</div>';grid.querySelectorAll('.anime-card').forEach(card=>{card.addEventListener('mouseenter',()=>showHoverCard(state.cardData.get(Number(card.dataset.animeId)),card));card.addEventListener('mouseleave',queueHideHoverCard);card.addEventListener('focus',()=>showHoverCard(state.cardData.get(Number(card.dataset.animeId)),card));card.addEventListener('blur',queueHideHoverCard)})}
function showHoverCard(anime,card){if(!state.settings.hoverCards||!anime||!window.matchMedia('(hover:hover)').matches)return;clearTimeout(state.hoverTimer);let pop=document.getElementById('anime-hover-card');if(!pop){pop=document.createElement('aside');pop.id='anime-hover-card';pop.className='anime-hover-card';pop.addEventListener('mouseenter',()=>clearTimeout(state.hoverTimer));pop.addEventListener('mouseleave',queueHideHoverCard);document.body.appendChild(pop)}const title=titleOf(anime),image=anime.bannerImage||anime.trailer?.thumbnail||coverOf(anime),rect=card.getBoundingClientRect();pop.dataset.anchorId=card.dataset.animeId;pop.innerHTML=`<div class="hover-art"><img src="${escapeAttribute(image)}" alt=""><span>${escapeHTML(anime.status||'ANIME')}</span></div><div class="hover-content"><h3>${escapeHTML(title)}</h3><p class="hover-native">${escapeHTML(anime.title.native||anime.title.romaji||'')}</p><div class="hover-stats"><span>${anime.episodes||'?'} EPS</span><span><svg class=\"hs-icon\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>${anime.averageScore?anime.averageScore/10:'—'}</span><span><svg class=\"hs-icon\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><path d=\"M18 20a6 6 0 0 0-12 0\"/><circle cx=\"12\" cy=\"10\" r=\"4\"/></svg>${compactNumber(anime.popularity)}</span><span><svg class=\"hs-icon\" viewBox=\"0 0 24 24\" aria-hidden=\"true\"><rect width=\"20\" height=\"15\" x=\"2\" y=\"3\" rx=\"2\"/><polyline points=\"8 21 12 17 16 21\"/></svg>${escapeHTML(anime.format||'ANIME')}</span></div><p class="hover-genres">${escapeHTML((anime.genres||[]).slice(0,4).join(' · '))}</p>${anime.nextAiringEpisode?`<p class="hover-airing">NEXT · Episode ${anime.nextAiringEpisode.episode} ${relativeAiring(anime.nextAiringEpisode.airingAt)}</p>`:''}<button class="hover-open" type="button">View title <span>→</span></button></div>`;pop.querySelector('.hover-open').addEventListener('click',event=>{event.stopPropagation();hideHoverCard();openAnime(anime.id)});const box=pop.getBoundingClientRect(),width=box.width,height=box.height;let left=rect.left-48;if(left+width>window.innerWidth-14)left=window.innerWidth-width-14;if(left<14)left=14;const top=Math.max(72,Math.min(rect.top-18,window.innerHeight-height-14));pop.style.left=`${left+window.scrollX}px`;pop.style.top=`${top+window.scrollY}px`;requestAnimationFrame(()=>pop.classList.add('is-visible'))}
function queueHideHoverCard(){clearTimeout(state.hoverTimer);state.hoverTimer=setTimeout(hideHoverCard,130)}function hideHoverCard(){clearTimeout(state.hoverTimer);const pop=document.getElementById('anime-hover-card');if(pop){pop.classList.remove('is-visible');setTimeout(()=>{if(!pop.classList.contains('is-visible'))pop.remove()},130)}}function compactNumber(value){return Number(value||0)>999?`${(Number(value)/1000).toFixed(1)}k`:Number(value||0).toString()}function relativeAiring(timestamp){if(!timestamp)return'';const hours=Math.max(0,Math.round((timestamp*1000-Date.now())/3600000));return hours<24?`in ${hours}h`:`in ${Math.round(hours/24)}d`}
function renderDetail(){const a=state.currentAnime,title=titleOf(a),total=Number(a.episodes)||12,entries=a.episodeEntries||episodeEntries(a);const options=Object.entries(PLAYER_SOURCES).map(([key,s])=>`<option value="${key}" ${key===state.source?'selected':''} ${s.build(a,1,state.language,state.settings.autoplay)?'':'disabled'}>${s.label}${s.build(a,1,state.language,state.settings.autoplay)?'':' (unavailable)'}</option>`).join('');const year=a.startDate?.year||a.seasonYear||'—',banner=a.bannerImage||coverOf(a),studios=(a.studios?.nodes||[]).map(s=>s.name).join(', '),progress=a.mediaListEntry?.progress||0;document.getElementById('detail-content').innerHTML=`<div class="detail-hero"><img class="detail-banner-glow" src="${escapeAttribute(banner)}" alt=""><div class="detail-banner">${a.bannerImage?`<img src="${escapeAttribute(a.bannerImage)}" alt="">`:''}</div></div><div class="detail-layout"><img class="detail-poster" src="${escapeAttribute(coverOf(a))}" alt="${escapeAttribute(title)}"><div class="detail-body"><p class="detail-romaji">${escapeHTML(a.title.romaji||title)}</p><h1>${escapeHTML(a.title.english||title)}</h1><p class="detail-subtitle">${escapeHTML(a.title.native||'')}</p><div class="metadata"><span>${escapeHTML(a.format||'ANIME')}</span><span>${total} episodes</span>${a.duration?`<span>${a.duration} min</span>`:''}<span>${escapeHTML(a.status||'UNKNOWN')}</span><span>${year}</span>${a.averageScore?`<span>★ ${a.averageScore}/100</span>`:''}<span>${Number(a.popularity||0).toLocaleString()} users</span></div>${state.auth.viewer?`<div class="anilist-progress"><span>AniList progress</span><strong>${progress} / ${total||'?'}</strong></div>`:''}${studios?`<p class="detail-fact"><b>Studio</b> ${escapeHTML(studios)}</p>`:''}${a.source?`<p class="detail-fact"><b>Source</b> ${escapeHTML(a.source.replace(/_/g,' '))}${a.countryOfOrigin?` · ${escapeHTML(a.countryOfOrigin)}`:''}</p>`:''}<div class="genre-list">${(a.genres||[]).map(g=>`<span class="genre">${escapeHTML(g)}</span>`).join('')}</div><div class="description">${escapeHTML(a.description||'No synopsis is available for this title.')}</div><div class="external-links"><a href="https://anilist.co/anime/${a.id}" target="_blank" rel="noreferrer">AniList ↗</a>${a.idMal?`<a href="https://myanimelist.net/anime/${a.idMal}" target="_blank" rel="noreferrer">MyAnimeList ↗</a>`:''}${a.trailer?.site==='youtube'?`<a href="https://www.youtube.com/watch?v=${a.trailer.id}" target="_blank" rel="noreferrer">Trailer ↗</a>`:''}</div></div></div><section class="watch-area"><div id="player-card" class="player-card is-active"><div class="player-toolbar"><strong>Web player</strong><span>Select an episode below to begin</span></div><div class="player-placeholder"><span>▶</span><p>${escapeHTML(title)}</p><small>Choose an episode to load your selected provider.</small></div></div><div class="watch-header"><h2>Episodes</h2><div class="watch-controls"><div class="language-toggle"><button class="${state.language==='sub'?'is-active':''}" type="button" data-language="sub">Sub</button><button class="${state.language==='dub'?'is-active':''}" type="button" data-language="dub">Dub</button></div><select id="source-select" class="source-select" aria-label="Playback source">${options}</select></div></div><div class="episode-toolbar"><p>${entries.some(entry=>entry.mapped)?'Mapped episode titles are available.':'Episode titles unavailable — using Episode {num}.'}</p><input id="episode-filter" class="episode-filter" type="search" placeholder="Filter episode title or number"></div><div id="episode-grid" class="episode-grid">${entries.map(episodeButton).join('')}</div><p class="player-note">Ryuu loads providers directly in an iframe. If a provider is unavailable for an episode or language, select another source.</p></section>${relationsMarkup(a)}`;document.querySelectorAll('[data-language]').forEach(b=>b.addEventListener('click',()=>{state.language=b.dataset.language;renderDetail()}));document.getElementById('source-select').addEventListener('change',e=>{state.source=e.target.value;if(document.querySelector('.player-frame'))playEpisode(state.currentEpisode)});document.querySelectorAll('.episode-button').forEach(b=>b.addEventListener('click',()=>playEpisode(Number(b.dataset.episode))));document.getElementById('episode-filter').addEventListener('input',filterEpisodes)}
function episodeButton(entry){return `<button class="episode-button ${entry.number===state.currentEpisode?'is-active':''}" type="button" data-episode="${entry.number}" data-search="${escapeAttribute(`${entry.number} ${entry.title}`.toLowerCase())}" title="${escapeAttribute(entry.title)}"><b>${entry.number}</b><span>${escapeHTML(entry.title)}</span></button>`}function filterEpisodes(e){const term=e.target.value.trim().toLowerCase();document.querySelectorAll('.episode-button').forEach(b=>b.hidden=!!term&&!b.dataset.search.includes(term))}function playEpisode(e){state.currentEpisode=e;const url=PLAYER_SOURCES[state.source].build(state.currentAnime,e,state.language,state.settings.autoplay);if(!url){toast('This source needs a MyAnimeList ID, which is not available for this title.',true);return}const p=document.getElementById('player-card');p.classList.add('is-active');p.innerHTML=`<div class="player-toolbar"><strong>${escapeHTML(titleOf(state.currentAnime))} · Episode ${e}</strong><span>${PLAYER_SOURCES[state.source].label} · ${state.language.toUpperCase()}</span>${state.auth.viewer?`<button id="mark-watched" class="mark-watched" type="button">Mark watched</button>`:''}</div><iframe class="player-frame" src="${escapeAttribute(url)}" title="${escapeAttribute(titleOf(state.currentAnime))} episode ${e}" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>`;document.getElementById('mark-watched')?.addEventListener('click',()=>markEpisodeWatched(e));document.querySelectorAll('.episode-button').forEach(b=>b.classList.toggle('is-active',Number(b.dataset.episode)===e));p.scrollIntoView({behavior:'smooth',block:'center'})}async function markEpisodeWatched(episode){if(!state.auth.token)return;const button=document.getElementById('mark-watched');if(button){button.disabled=true;button.textContent='Saving…'}try{const data=await anilist(`mutation SaveProgress($mediaId: Int, $progress: Int) { SaveMediaListEntry(mediaId: $mediaId, progress: $progress, status: CURRENT) { id progress status } }`,{mediaId:state.currentAnime.id,progress:episode});state.currentAnime.mediaListEntry=data.SaveMediaListEntry;toast(`AniList updated to episode ${episode}.`);renderDetail();playEpisode(episode)}catch(error){if(button){button.disabled=false;button.textContent='Mark watched'}toast(`Could not update AniList: ${error.message}`,true)}}
function renderSettings(){const s=state.settings,cards=[['glow','Glow effect','Add a subtle atmospheric glow to banner images and featured artwork.'],['showHero','Show home hero','Keep the oversized Ryuu introduction at the top of the home page.'],['hoverCards','Hover detail cards','Show the Zenshin-style title details popup when hovering over anime cards.'],['reducedMotion','Reduce motion','Turn off carousel and interface animations.'],['autoplay','Autoplay in web player','Request autoplay when the selected provider supports it.']];document.getElementById('settings-content').innerHTML=`${anilistAccountMarkup()}${cards.map(([k,h,d])=>`<article class="setting-card"><div><h2>${h}</h2><p>${d}</p></div><input class="switch" type="checkbox" data-setting="${k}" ${s[k]?'checked':''} aria-label="${h}"></article>`).join('')}<article class="setting-card"><div><h2>Default audio</h2><p>Choose which language is preselected on a title page.</p></div><select class="setting-choice" data-setting="defaultLanguage"><option value="sub" ${s.defaultLanguage==='sub'?'selected':''}>Japanese · Sub</option><option value="dub" ${s.defaultLanguage==='dub'?'selected':''}>English · Dub</option></select></article><article class="setting-card"><div><h2>Preferred playback source</h2><p>This source is selected before you start an episode. You can always change it per title.</p></div><select class="setting-choice" data-setting="preferredSource">${Object.entries(PLAYER_SOURCES).map(([k,v])=>`<option value="${k}" ${s.preferredSource===k?'selected':''}>${v.label}</option>`).join('')}</select></article>`;document.querySelectorAll('[data-setting]').forEach(c=>c.addEventListener('change',()=>{state.settings[c.dataset.setting]=c.type==='checkbox'?c.checked:c.value;saveSettings();applySettings();toast('Settings saved.')}))}
function applySettings(){document.documentElement.classList.toggle('reduced-motion',state.settings.reducedMotion);document.body.classList.toggle('no-glow',!state.settings.glow);document.getElementById('landing-hero')?.toggleAttribute('hidden',!state.settings.showHero);if(!state.settings.hoverCards)hideHoverCard();if(state.settings.reducedMotion)stopFeatureAutoplay();else if(state.featured.length)startFeatureAutoplay()}function saveSettings(){localStorage.setItem('ryuu-settings',JSON.stringify(state.settings))}function showRoute(route){hideHoverCard();document.querySelectorAll('.view').forEach(v=>v.classList.toggle('is-active',v.id===`${route}-view`));state.route=route;window.scrollTo({top:0,behavior:state.settings.reducedMotion?'auto':'smooth'})}
async function anilist(query,variables){const headers={'Content-Type':'application/json',Accept:'application/json'};if(state.auth.token)headers.Authorization=`Bearer ${state.auth.token}`;const response=await fetch(API_URL,{method:'POST',headers,body:JSON.stringify({query,variables})});const payload=await response.json();if(!response.ok||payload.errors)throw new Error(payload.errors?.[0]?.message||'AniList request failed');return payload.data}function episodeEntries(anime){const total=Math.max(Number(anime.episodes)||0,...(anime.streamingEpisodes||[]).map((episode,index)=>Number((episode.title||'').match(/\d+(?:\.\d+)?/)?.[0])||index+1),1);const mapped=new Map();(anime.streamingEpisodes||[]).forEach((episode,index)=>{const number=Number((episode.title||'').match(/\d+(?:\.\d+)?/)?.[0])||index+1;mapped.set(number,{number,title:episode.title||`Episode ${number}`,mapped:true,thumbnail:episode.thumbnail||''})});return Array.from({length:total},(_,index)=>mapped.get(index+1)||{number:index+1,title:`Episode ${index+1}`,mapped:false})}function anilistAccountMarkup(){const viewer=state.auth.viewer;if(!viewer)return `<article class="setting-card anilist-account"><div><h2>AniList account</h2><p>Connect to show your progress and mark completed episodes from Ryuu.</p></div><button class="button button-primary" data-anilist-auth type="button">Connect AniList</button></article>`;const stats=viewer.statistics?.anime;return `<article class="setting-card anilist-account"><div class="anilist-user">${viewer.avatar?.large?`<img src="${escapeAttribute(viewer.avatar.large)}" alt="">`:''}<div><h2>${escapeHTML(viewer.name)}</h2><p>AniList connected · ${stats?.episodesWatched||0} episodes watched · ${stats?.minutesWatched||0} minutes watched</p></div></div><button class="button button-quiet" data-anilist-auth type="button">Disconnect</button></article>`}function relationsMarkup(anime){const relations=(anime.relations?.edges||[]).filter(edge=>edge.node?.type==='ANIME').slice(0,8);if(!relations.length)return'';return `<section class="relations-section"><div class="relations-heading"><p class="eyebrow">ANILIST</p><h2>Related anime</h2></div><div class="relations-grid">${relations.map(edge=>`<article class="relation-card" data-anime-id="${edge.node.id}" role="button" tabindex="0"><img src="${escapeAttribute(edge.node.coverImage?.medium||'./assets/favicon.png')}" alt=""><div><span>${escapeHTML(edge.relationType.replace(/_/g,' '))}</span><strong>${escapeHTML(edge.node.title.userPreferred||'Untitled')}</strong><small>${escapeHTML(edge.node.format||'ANIME')} · ${edge.node.episodes||'?'} eps ${edge.node.seasonYear?`· ${edge.node.seasonYear}`:''}</small></div></article>`).join('')}</div></section>`}function titleOf(a){return a.title?.english||a.title?.romaji||a.title?.native||'Untitled'}function coverOf(a){return a.coverImage?.extraLarge||a.coverImage?.large||a.coverImage?.medium||'./assets/favicon.png'}function trimText(t,l){const clean=(t||'').replace(/\s+/g,' ').trim();return clean.slice(0,l)+(clean.length>l?'…':'')}function escapeHTML(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function escapeAttribute(v=''){return escapeHTML(v)}function statsMarkup(a){return `<div class="anime-stats"><span>${a.episodes||'?'} EPS</span><span>★ ${a.averageScore||'—'}</span><span>${Number(a.popularity||0).toLocaleString()} USERS</span></div>`}function readStored(k,f){try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}}function toast(m,error=false){const r=document.getElementById('toast-region'),i=document.createElement('div');i.className=`toast ${error?'error':''}`;i.textContent=m;r.appendChild(i);setTimeout(()=>i.remove(),3800)}function showNetworkError(ids,error){ids.forEach(id=>{const n=document.getElementById(id);if(n)n.innerHTML=`<div class="empty-state">Could not load AniList data. ${escapeHTML(error.message)}</div>`})}function registerServiceWorker(){if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}))}
